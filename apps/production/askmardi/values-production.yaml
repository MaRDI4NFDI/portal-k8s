apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: askmardi
spec:
  chart:
    spec:
      version: 0.1.1
  values:
    resources:
      requests:
        memory: "1Gi"
        cpu: "1"
      limits:
        memory: "4Gi"
        cpu: "3"
