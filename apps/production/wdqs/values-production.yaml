apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wdqs
spec:
  chart:
    spec:
      version: "0.1.19"
  values:
    wdqs:
      storage: 200Gi
      memory: "-Xmx16g -Xms16g"
      heap_size: "16g"
      opts: "-Dorg.wikidata.query.rdf.tool.rdf.RdfRepository.timeout=3600"
      resources:
        requests:
          cpu: 1000m
          memory: 48Gi
        limits:
          cpu: 4000m
          memory: 48Gi
    updater:
      enabled: true
      resources:
        requests:
          cpu: 200m
          memory: 1Gi
        limits:
          cpu: 1000m
          memory: 2Gi

    frontend:
      replicas: 1
      resources:
        requests:
          cpu: 50m
          memory: 128Mi
        limits:
          cpu: 500m
          memory: 512Mi

