apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: staging-matomo
spec:
  values:
    matomoUsername: "admin"
    matomoEmail: "admin@example.com"
    matomoWebsiteName: "MaRDI Portal"
    matomoWebsiteHost: "matomo.portal.mardi4nfdi.de"
    persistence:
      enabled: true
      size: 10Gi