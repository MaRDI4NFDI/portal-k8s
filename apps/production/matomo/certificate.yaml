apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: matomo-tls
spec:
  secretName: matomo-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - matomo.portal.mardi4nfdi.de
  duration: 2160h # 90d
  renewBefore: 360h # 15d