apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: change-propagation
spec:
  chart:
    spec:
      version: "0.1.4"
  values:
    mediawiki:
      host: http://wikibase-jobrunner
    jobqueue:
      concurrency: 18