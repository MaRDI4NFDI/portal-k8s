apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prefect-tls
spec:
  secretName: prefect-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - prefect.portal.mardi4nfdi.de
  duration: 2160h # 90d
  renewBefore: 360h # 15d