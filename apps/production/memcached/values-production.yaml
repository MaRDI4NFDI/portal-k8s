apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: memcached
spec:
  values:
    resources:
      requests:
        memory: 4Gi
        cpu: 1000m
      limits:
        memory: 12Gi
        cpu: 4000m