apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: maintenance
spec:
  chart:
    spec:
      version: "0.1.1"
  values:
    maintenance:
      enabled: false
    target:
      name: "wikibase"

