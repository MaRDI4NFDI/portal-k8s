apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: latexml
spec:
  chart:
    spec:
      version: "0.1.5"
  values:
    ingress:
      enabled: true
    resources:
      requests:
        memory: 256Mi
        cpu: 100m
      limits:
        memory: 2Gi
        cpu: 2000m

