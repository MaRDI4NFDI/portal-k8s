apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: prod-latexml
  namespace: production
spec:
  chart:
    spec:
      version: "0.1.3"
  values:
    ingress:
      enabled: false

