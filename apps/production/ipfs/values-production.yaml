apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ipfs
spec:
  chart:
    spec:
      version: "0.2.0"
  values:
    admin:
      enabled: true
      existingSecret: ipfs-oauth2-proxy
