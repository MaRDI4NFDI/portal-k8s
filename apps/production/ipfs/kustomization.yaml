apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/ipfs
  - secrets.yaml

patches:
  - path: values-production.yaml
    target:
      kind: HelmRelease
      name: ipfs
