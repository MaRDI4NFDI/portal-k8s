apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: qdrant
spec:
  values:
    replicaCount: 1    
    
    resources:
      requests:
        cpu: 2000m
        memory: 6Gi
      limits:
        cpu: 6000m
        memory: 12Gi
    
    persistence:
      size: 100Gi
            
    
