apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: staging-traefik
  namespace: staging
spec:
  chart:
    spec:
      version: "34.4.1"
  values:
    ingressRoute:
      dashboard:
        enabled: true
    logs:
      general:
        level: DEBUG
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: staging-traefik-crds
  namespace: staging
spec:
  chart:
    spec:
      version: "1.5.0"
    