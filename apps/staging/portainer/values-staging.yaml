apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: staging-portainer
  namespace: staging
spec:
  values:
    image:
      repository: portainer/portainer-ce
      tag: 2.27.2
      pullPolicy: Always
    service:
      type: ClusterIP
    persistence:
      size: "2Gi"
    