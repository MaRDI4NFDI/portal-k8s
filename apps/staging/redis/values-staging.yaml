apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis
spec:
  values:
    persistence:
      size: 1Gi
    resources:
      limits:
        cpu: 100m
        memory: 256Mi
      requests:
        cpu: 500m
        memory: 512Mi