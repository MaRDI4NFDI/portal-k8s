apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kafka
  namespace: staging
spec:
  chart:
    spec:
      version: "0.1.6"
  values:
    replicas: 1
    replicationFactor: 1
    minIsr: 1