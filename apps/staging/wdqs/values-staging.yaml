apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: staging-wdqs
  namespace: staging
spec:
  chart:
    spec:
      version: "0.1.11"
  values:
    wdqs:
      host: staging-wdqs
    ingress:
      enabled: true