apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: staging-maintenance
spec:
  chart:
    spec:
      version: "0.1.0"
  values:
    maintenance:
      enabled: true
    target:
      name: "staging-wikibase"

