apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: change-propagation
  namespace: staging
spec:
  chart:
    spec:
      version: "0.1.3"
  values:
    mediawiki:
      host: http://staging-wikibase-jobrunner
    kafka:
      replicas: 1


