apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: staging-qlever
  namespace: staging
spec:
  chart:
    spec:
      version: "0.1.21"
  values:
    qlever:
      data:
        name: mardi-staging
        description: MaRDI Knowledge Graph in Staging
    ingress:
      enabled: true
    updater:
      enabled: true