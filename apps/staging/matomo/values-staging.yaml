apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: staging-matomo
spec:
  values:
    matomoUsername: "admin"
    matomoEmail: "admin@example.com"
    matomoWebsiteName: "Staging MaRDI Portal"
    matomoWebsiteHost: "matomo.staging.mardi4nfdi.org"
    existingSecret: "staging-matomo-secrets"
    externalDatabase:
      host: "staging-mariadb-matomo"
      existingSecret: "staging-matomo-secrets"