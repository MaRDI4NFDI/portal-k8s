apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: anubis
spec:
  interval: 5m
  chart:
    spec:
      chart: anubis
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system