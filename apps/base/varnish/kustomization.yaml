apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - release.yaml

configMapGenerator:
  - name: varnish-vcl
    files:
      - default.vcl
    options:
      disableNameSuffixHash: true