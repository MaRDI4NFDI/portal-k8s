apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: eventgate
spec:
  interval: 5m
  chart:
    spec:
      chart: eventgate
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system