apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: eventstreams
spec:
  interval: 5m
  chart:
    spec:
      chart: eventstreams
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system