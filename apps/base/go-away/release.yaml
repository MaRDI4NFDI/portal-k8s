apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: go-away
spec:
  interval: 5m
  chart:
    spec:
      chart: go-away
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system
