apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ipfs
spec:
  interval: 5m
  chart:
    spec:
      chart: ipfs
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system
