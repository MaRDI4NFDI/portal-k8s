apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: msc-services
spec:
  interval: 5m
  chart:
    spec:
      chart: msc-services
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system
