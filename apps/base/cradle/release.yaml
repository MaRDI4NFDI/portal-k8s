apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cradle
spec:
  interval: 5m
  chart:
    spec:
      chart: cradle
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system