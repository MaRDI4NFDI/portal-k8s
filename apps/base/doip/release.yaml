apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: doip
spec:
  interval: 5m
  chart:
    spec:
      chart: doip
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system