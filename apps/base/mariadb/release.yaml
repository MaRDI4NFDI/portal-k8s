apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mariadb
spec:
  interval: 5m
  chart:
    spec:
      chart: mariadb
      version: "0.1.1"
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system