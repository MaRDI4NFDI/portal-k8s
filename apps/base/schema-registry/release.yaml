apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: schema-registry
spec:
  interval: 5m
  chart:
    spec:
      chart: schema-registry
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system