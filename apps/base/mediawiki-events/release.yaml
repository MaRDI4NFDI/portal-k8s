apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mediawiki-events
spec:
  interval: 5m
  chart:
    spec:
      chart: mediawiki-events
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system