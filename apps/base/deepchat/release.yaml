apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: deepchat
spec:
  interval: 5m
  chart:
    spec:
      chart: deepchat
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system