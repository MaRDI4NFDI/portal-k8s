apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: qdrant
spec:
  interval: 30m
  chart:
    spec:
      chart: qdrant
      version: "1.16.2" 
      sourceRef:
        kind: HelmRepository
        name: qdrant
        namespace: flux-system
      interval: 12h