apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: demoservice
spec:
  interval: 5m
  chart:
    spec:
      chart: demoservice
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system