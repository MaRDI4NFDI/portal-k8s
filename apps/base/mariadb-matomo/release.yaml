apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mariadb-matomo
spec:
  interval: 5m
  chart:
    spec:
      chart: mariadb-matomo
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system