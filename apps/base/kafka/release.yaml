apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kafka
spec:
  interval: 5m
  chart:
    spec:
      chart: kafka
      sourceRef:
        kind: HelmRepository
        name: portal-k8s
        namespace: flux-system