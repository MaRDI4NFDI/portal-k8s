apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Release.Name }}
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ .Values.ingress.host | default .Values.global.baseDomain }}`)
      kind: Rule
      services:
        - name: {{ .Release.Name }}
          port: {{ .Values.goaway.servicePort }}
  tls:
    secretName: {{ .Release.Name }}-tls
