apiVersion: v2
name: quickstatements
description: A Helm chart for Quickstatements
type: application
version: 0.1.4
appVersion: "1.0.0"
annotations:
  dependencies: |
    - Secret: wikibase-secrets (OAUTH_CONSUMER_KEY, OAUTH_CONSUMER_SECRET)