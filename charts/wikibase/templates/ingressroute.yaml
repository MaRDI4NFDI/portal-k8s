{{- if .Values.ingress.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Release.Name }}
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ .Values.global.baseDomain }}`)
      kind: Rule
      {{- if or .Values.blockHeaders.enabled .Values.rateLimit.enabled}}
      middlewares:
        {{- if .Values.blockHeaders.enabled }}
        - name: {{ .Release.Name }}-block-fake-agents
          namespace: {{ .Release.Namespace }}
        {{- end }}
        {{- if .Values.fail2ban.enabled }}
        - name: {{ .Release.Name }}-fail2ban
          namespace: {{ .Release.Namespace }}
        {{- end }}
      {{- end }}
      services:
        - name: {{ .Release.Name }}
          namespace: {{ .Release.Namespace }}
          port: {{ .Values.port }}
  tls:
    secretName: {{ .Release.Name }}-tls
{{- end }}