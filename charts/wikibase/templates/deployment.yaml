apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  replicas: {{ .Values.mediawiki.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
      annotations:
        kubectl.kubernetes.io/default-container: "mediawiki"
    spec:
      securityContext:
        fsGroup: 33 # www-data
      containers:
        - name: apache
          image: "{{ .Values.apache.image.repository }}:{{ .Values.mediawiki.image.tag }}"
          imagePullPolicy: {{ .Values.apache.image.pullPolicy }}
          resources:
            requests:
              cpu: {{ .Values.apache.resources.requests.cpu }}
              memory: {{ .Values.apache.resources.requests.memory }}
            limits:
              cpu: {{ .Values.apache.resources.limits.cpu }}
              memory: {{ .Values.apache.resources.limits.memory }}
          ports:
            - containerPort: 80
          env:
            - name: WIKIBASE_HOST
              value: "127.0.0.1"
            - name: APACHE_MAX_REQUEST_WORKERS
              value: {{ .Values.mediawiki.phpfpm.maxChildren | quote }}
            - name: APACHE_LISTEN_BACKLOG
              value: {{ .Values.mediawiki.phpfpm.maxChildren | quote }}
          startupProbe:
            httpGet:
              path: /w/api.php?action=query&meta=siteinfo&format=json
              port: 80
            failureThreshold: 30
            periodSeconds: 10
          #readinessProbe:
          #  httpGet:
          #    path: /w/api.php?action=query&meta=siteinfo&format=json
          #    port: 80
          #  periodSeconds: 10
        {{- if .Values.apache.metrics.enabled }}
        - name: apache-exporter
          image: "{{ .Values.apache.metrics.image.repository }}:{{ .Values.apache.metrics.image.tag }}"
          ports:
            - name: apache-metrics
              containerPort: 9117
          resources:
            {{- toYaml .Values.apache.metrics.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          readinessProbe:
            tcpSocket:
              port: apache-metrics
          livenessProbe:
            tcpSocket:
              port: apache-metrics
        {{- end }}
        - name: mediawiki
          image: "{{ .Values.mediawiki.image.repository }}:{{ .Values.mediawiki.image.tag }}"
          imagePullPolicy: {{ .Values.mediawiki.image.pullPolicy }}
          resources:
            requests:
              cpu: {{ .Values.mediawiki.resources.requests.cpu }}
              memory: {{ .Values.mediawiki.resources.requests.memory }}
            limits:
              cpu: {{ .Values.mediawiki.resources.limits.cpu }}
              memory: {{ .Values.mediawiki.resources.limits.memory }}
          ports:
            - containerPort: {{ .Values.mediawiki.port }}
          volumeMounts:
            {{- include "mediawiki.volumeMounts" . | nindent 12 }}
          env:
            {{- include "mediawiki.env" . | nindent 12 }}
            {{- include "mediawiki.phpfpmEnv" .Values.mediawiki.phpfpm | nindent 12 }}
            {{- include "mediawiki.opcacheEnv" .Values.mediawiki.opcache | nindent 12 }}
          envFrom:
            {{- include "mediawiki.envFrom" . | nindent 12 }}
          livenessProbe:
            exec:
              command:
                - php
                - /var/www/html/w/maintenance/run.php
                - view
                - Portal
            initialDelaySeconds: 120
            periodSeconds: 300
            timeoutSeconds: 30
            failureThreshold: 2
      volumes:
        {{- include "mediawiki.volumes" . | nindent 8 }}
