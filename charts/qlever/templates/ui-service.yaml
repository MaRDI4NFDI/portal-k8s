apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ui
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.qlever-ui.port }}
      targetPort: {{ .Values.qlever-ui.port }}
  selector:
    app: {{ .Release.Name }}-ui