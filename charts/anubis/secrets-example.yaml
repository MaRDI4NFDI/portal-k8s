# This file documents the required secrets for this chart
# The actual secrets should be created externally (e.g., via Kustomize)
# and must be named anubis-key

apiVersion: v1
kind: Secret
metadata:
  name: anubis-key
type: Opaque
data:
  ED25519_PRIVATE_KEY_HEX: <base64-encoded-private-key>


  