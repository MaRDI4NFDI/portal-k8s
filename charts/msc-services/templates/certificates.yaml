{{- range .Values.services }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .name }}-tls
spec:
  secretName: {{ .name }}-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - {{ .name }}.{{ $.Values.global.baseDomain }}
{{- end }}
